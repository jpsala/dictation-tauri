# Product Route Disposition — Checkpoint D / D-R1

Status: **Normative — D1 COMPLETE and ready for TDD**. This map matches the current source inventory, including the supported Control Room BFF consumer for `admin-profile-apply` (`POST /admin/control-plane/profiles/apply`). Canonical schemas and alias rules are normative; implementation has not started.

## Decision Rules And Counts

- Inventory source: `HTTP_CONTRACT_FIXTURES` in `tests/cloud-contract/fixtures.ts`.
- Exactly **74 HTTP fixtures**, **73 method/path pairs**, two explicit `GET /desktop/login` scenarios, and **1 scheduled boundary**.
- Dispositions: **1 canonical**, **9 redesign**, **40 temporary-compat**, **24 drop**.
- `canonical` is reserved for an exact product-owned route. `redesign` retains the capability but not the route. `temporary-compat` has a current consumer, replacement, owner, and removal condition. `drop` has no current Dictation Tauri/Control Room consumer and is outside the target.
- Query values below are synthetic fixture evidence. They are not operator data.

## Tauri Consumer Map

| Current URL | Flow / caller | Payload dependency | Migration direction |
| --- | --- | --- | --- |
| `POST /v2/device/register` | register/refresh in `fixvox_cloud.rs` | install/device metadata; policy projection | alias to canonical desktop bootstrap/context; moderate client migration |
| `POST /v2/device/activate` | invite activation in `fixvox_cloud.rs` | invite + device metadata | fold into canonical desktop account/device linking; moderate |
| `POST /v2/execution/preflight` | `preflight_endpoint`; called from runtime transcription | device + operation metadata, no content | redesign as internal authoritative admission immediately before provider; moderate |
| `GET /desktop/login` | `build_fixvox_login_verification_url` | device-code flow/client/state | alias during canonical desktop auth migration; moderate |
| `GET /desktop/login/status` | `build_fixvox_login_status_url` | state nonce | alias during session handoff migration; moderate |
| `POST /desktop/login/link-device` | `link_signed_in_session_device_*` | session handoff + device metadata | alias to canonical authenticated device link; moderate |
| `POST /v1/audio/transcriptions` | `build_managed_stt_request_preview`; runtime transcription | multipart audio + hints; server owns provider/model | replace with canonical transcription; high because hot path/quota boundary |
| `POST /v1/chat/completions` | `build_managed_chat_completion_request_preview` | transcript + engine kind; current body still resembles generic chat | replace with typed action; high because action semantics/provider authority |

No Tauri caller was found for telemetry, feedback, usage-prewarm, direct `/auth/google/*`, support, Admin, pricing, or usage-counter fetch routes.

## Control Room BFF Map

The browser contract remains product-owned at `/api/admin/*`; only the downstream backend path is migration inventory.

| Browser route(s) | Downstream `proxyAdmin(...)` | Product domain |
| --- | --- | --- |
| auth role resolution for every guarded request | `GET /admin/control-plane/roles/resolve` | RBAC |
| `GET /api/admin/audit` | `GET /admin/control-plane/audit` | Audit |
| `GET/POST /api/admin/roles`, `POST /api/admin/roles/remove` | role list/set/remove | RBAC |
| `POST/PUT/DELETE /api/admin/profiles/drafts` | same methods on `/admin/control-plane/profiles/drafts` | Profiles |
| `GET /api/admin/profiles/preview` | `GET /admin/control-plane/profiles/preview` | Profiles |
| `POST /api/admin/profiles/apply` | `POST /admin/control-plane/profiles/apply` with server-owned actor and credential | Profiles |
| `POST /api/admin/profiles/publish | rollback` | matching publish/rollback route with server-owned credential | Profiles |
| `GET /api/admin/accounts` | `GET /admin/control-plane/accounts` | Accounts |
| account policy/budget/groups mutations | matching downstream routes | Accounts/targeting |
| account segments or variants assignment | `POST /admin/control-plane/accounts/variants/assign` | Accounts/targeting |
| groups and account variant create/delete | matching downstream routes | Groups/targeting |
| policy variants and selection-preset defaults | matching downstream policy routes | Configuration |
| `GET /api/admin/pricing`, `POST /api/admin/pricing/refresh` | matching pricing routes | Engines/Pricing |
| engine and prompt create/delete | matching downstream routes | Engines/Prompts |
| `GET /api/admin/devices`, device policy mutation | matching downstream routes | Devices |
| `GET /api/admin/policies` | policy + first account + pricing fan-out | Configuration projection |
| `GET /api/admin/usage` | `GET /admin/usage/summary` | Usage |

Evidence: `admin/fixvox-web/server.mjs` `proxyAdmin`, `resolveServerRole`, and request-router branches. Browser-only `/api/admin/env`, `/api/admin/rbac`, and Pi Chat routes do not call the Worker and are outside the HTTP fixture inventory.

## HTTP Fixture Disposition (74/74)

| Fixture ID | Method/path | Real consumer | Product capability | Disposition | Canonical replacement | Retirement / completion condition | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `health` | `GET /health` | operations/deploy probes | operations | `canonical` | same operations contract (`GET /health`) | n/a | `tests/cloud-contract/fixtures.ts` (`health`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `control-plane-admin-page` | `GET /control-plane-admin` | none in current Tauri/Control Room | Admin surface | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`control-plane-admin-page`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-options-cors` | `OPTIONS /admin/control-plane/policy` | none in current Tauri/Control Room | Admin surface | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-options-cors`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-dashboard-summary` | `GET /admin/dashboard/summary` | product capability; no exact BFF route | usage/operations | `redesign` | Control Room usage/audit domain DTO | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`admin-dashboard-summary`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-request-events` | `GET /admin/requests?limit=5` | product capability; no exact BFF route | usage/operations | `redesign` | Control Room usage/audit domain DTO | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`admin-request-events`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-usage-summary` | `GET /admin/usage/summary?limit=5` | Control Room BFF | usage/operations | `temporary-compat` | Control Room usage/audit domain DTO | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-usage-summary`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-feedback-list` | `GET /admin/feedback?limit=5` | product capability; no exact BFF route | feedback | `redesign` | bounded/redacted product-signals contract | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`admin-feedback-list`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-runtime-policy` | `GET /admin/control-plane/policy` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-runtime-policy`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-profiles-list` | `GET /admin/control-plane/profiles` | product capability; no exact BFF route | profiles/configuration | `redesign` | Control Room Profiles domain API | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`admin-profiles-list`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-profile-audit` | `GET /admin/control-plane/audit` | Control Room BFF | audit | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-profile-audit`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-profile-preview` | `GET /admin/control-plane/profiles/preview?profileId=pro` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-profile-preview`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-role-resolve` | `GET /admin/control-plane/roles/resolve?bootstrapOwnerEmail=owner%40fixture.test&principalKey=fixture-principal` | Control Room BFF | RBAC | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-role-resolve`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-role-list` | `GET /admin/control-plane/roles?bootstrapOwnerEmail=owner%40fixture.test` | Control Room BFF | RBAC | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-role-list`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-role-set` | `POST /admin/control-plane/roles` | Control Room BFF | RBAC | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-role-set`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-role-remove` | `POST /admin/control-plane/roles/remove` | Control Room BFF | RBAC | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-role-remove`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-profile-draft-create` | `POST /admin/control-plane/profiles/drafts` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-profile-draft-create`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-profile-apply` | `POST /admin/control-plane/profiles/apply` | Control Room BFF via stable `POST /api/admin/profiles/apply` | Profiles | `temporary-compat` | `POST /product/v1/control-room/profiles/{profileKey}/apply` | owner: Control Room; remove when the BFF uses the replacement and the legacy backend consumer count is zero | focused test: `admin/fixvox-web/server.test.mjs` — `profile apply BFF overwrites browser actor and redacts broker failures`; rollback: `CF-BFF` |
| `admin-profile-publish` | `POST /admin/control-plane/profiles/publish` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-profile-publish`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-profile-rollback` | `POST /admin/control-plane/profiles/rollback` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-profile-rollback`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-devices-list` | `GET /admin/control-plane/devices?limit=5` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-devices-list`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-device-policy` | `POST /admin/control-plane/devices/policy` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-device-policy`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-accounts-list` | `GET /admin/control-plane/accounts?limit=5` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-accounts-list`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-account-policy` | `POST /admin/control-plane/accounts/policy` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-account-policy`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-account-budget` | `POST /admin/control-plane/accounts/budget` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-account-budget`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-account-groups` | `POST /admin/control-plane/accounts/groups` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-account-groups`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-account-segments` | `POST /admin/control-plane/accounts/segments` | product capability; no exact BFF route | accounts/devices targeting | `redesign` | `PUT /product/v1/control-room/configuration/targeting` | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`admin-account-segments`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-account-variants-assign` | `POST /admin/control-plane/accounts/variants/assign` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-account-variants-assign`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-groups-create` | `POST /admin/control-plane/groups` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-groups-create`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-account-variant-create` | `POST /admin/control-plane/accounts/variants` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-account-variant-create`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-account-variant-delete` | `POST /admin/control-plane/accounts/variants/delete` | Control Room BFF | accounts/devices targeting | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-account-variant-delete`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-policy-variants` | `POST /admin/control-plane/policy/variants` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-policy-variants`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-policy-engines-read-only-conflict` | `POST /admin/control-plane/policy/engines` | none in current Tauri/Control Room | profiles/configuration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-policy-engines-read-only-conflict`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-policy-budget-read-only-conflict` | `POST /admin/control-plane/policy/budget` | none in current Tauri/Control Room | profiles/configuration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-policy-budget-read-only-conflict`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-selection-preset-defaults` | `POST /admin/control-plane/policy/selection-presets` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-selection-preset-defaults`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-engine-create` | `POST /admin/control-plane/engines` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-engine-create`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-engine-delete` | `POST /admin/control-plane/engines/delete` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-engine-delete`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-prompt-create` | `POST /admin/control-plane/prompts` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-prompt-create`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-prompt-delete` | `POST /admin/control-plane/prompts/delete` | Control Room BFF | profiles/configuration | `temporary-compat` | product-owned Control Room domain API behind stable `/api/admin/*` BFF | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-prompt-delete`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-runtime-policy-put` | `POST /admin/control-plane/policy` | none in current Tauri/Control Room | profiles/configuration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-runtime-policy-put`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-runtime-policy-reset` | `POST /admin/control-plane/policy/reset` | none in current Tauri/Control Room | profiles/configuration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-runtime-policy-reset`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-recipe-policy-get` | `GET /admin/control-plane/recipe-policy` | none in current Tauri/Control Room | legacy recipe policy | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-recipe-policy-get`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-recipe-policy-put` | `POST /admin/control-plane/recipe-policy` | none in current Tauri/Control Room | legacy recipe policy | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-recipe-policy-put`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-recipe-policy-reset` | `POST /admin/control-plane/recipe-policy/reset` | none in current Tauri/Control Room | legacy recipe policy | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-recipe-policy-reset`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-provider-models` | `GET /admin/control-plane/models?provider=groq` | product capability; no exact BFF route | engines/pricing | `redesign` | Control Room Engines/Pricing domain API | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`admin-provider-models`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-pricing-get` | `GET /admin/pricing` | Control Room BFF | engines/pricing | `temporary-compat` | Control Room Engines/Pricing domain API | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-pricing-get`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-pricing-watchlist` | `POST /admin/pricing/watchlist` | none in current Tauri/Control Room | engines/pricing | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-pricing-watchlist`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-pricing-refresh` | `POST /admin/pricing/refresh` | Control Room BFF | engines/pricing | `temporary-compat` | Control Room Engines/Pricing domain API | owner: Control Room; remove after BFF uses the domain API and backend route count is zero | `tests/cloud-contract/fixtures.ts` (`admin-pricing-refresh`); `admin/fixvox-web/server.mjs` `proxyAdmin(...)` |
| `admin-discord-config` | `GET /admin/discord/config` | none in current Tauri/Control Room | support integration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-discord-config`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-discord-sync-commands` | `POST /admin/discord/sync-commands` | none in current Tauri/Control Room | support integration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-discord-sync-commands`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-discord-scan-support` | `POST /admin/discord/scan-support` | none in current Tauri/Control Room | support integration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-discord-scan-support`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `admin-benchmark-chat` | `POST /admin/benchmark/chat/completions` | none in current Tauri/Control Room | benchmark | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`admin-benchmark-chat`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `discord-interaction-invalid-signature` | `POST /discord/interactions` | none in current Tauri/Control Room | support integration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`discord-interaction-invalid-signature`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `telegram-webhook-disabled` | `POST /telegram/webhook` | none in current Tauri/Control Room | support integration | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`telegram-webhook-disabled`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `device-register` | `POST /v2/device/register` | Dictation Tauri | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`device-register`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `device-activate` | `POST /v2/device/activate` | Dictation Tauri | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`device-activate`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `execution-preflight` | `POST /v2/execution/preflight` | Dictation Tauri | runtime admission | `redesign` | admission inside canonical transcription/action request; optional advisory context only | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`execution-preflight`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `telemetry-events-batch` | `POST /v2/telemetry/events/batch` | future product capability; no exact client route | product signals | `redesign` | bounded/redacted product-signals contract | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`telemetry-events-batch`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `feedback-submit` | `POST /v2/feedback/submit` | future product capability; no exact client route | product signals | `redesign` | bounded/redacted product-signals contract | superseded by the D1 product contract; no exact-route compatibility promised | `tests/cloud-contract/fixtures.ts` (`feedback-submit`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `desktop-login-start` | `GET /desktop/login?flow=device-code&client=fixvox-tauri&state=fixture-oauth-state-001` | Dictation Tauri | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`desktop-login-start`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `desktop-login-invalid` | `GET /desktop/login?flow=unknown&client=fixvox-tauri` | Dictation Tauri | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`desktop-login-invalid`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `desktop-google-start-missing-handoff` | `GET /desktop/google/start?handoff=fixture-missing-handoff` | desktop OAuth browser flow | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Auth; remove only after canonical browser handoff/callback flow is deployed and old flow has zero consumers | `tests/cloud-contract/fixtures.ts` (`desktop-google-start-missing-handoff`); `cloud/fixvox-proxy/src/index.ts` desktop OAuth handlers |
| `desktop-login-status-missing` | `GET /desktop/login/status?state=fixture-oauth-state-missing` | Dictation Tauri | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`desktop-login-status-missing`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `desktop-link-device-missing-state` | `POST /desktop/login/link-device` | Dictation Tauri | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`desktop-link-device-missing-state`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `google-auth-start-json` | `GET /auth/google/start?device_id=fixture-device-001&code_verifier=fixture-code-verifier-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa&mode=json` | none in current Tauri/Control Room | desktop bootstrap/auth | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`google-auth-start-json`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `google-auth-result-missing` | `GET /auth/google/result?state=fixture-oauth-state-missing&device_id=fixture-device-001` | none in current Tauri/Control Room | desktop bootstrap/auth | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`google-auth-result-missing`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `google-callback-missing-state` | `GET /callback` | desktop OAuth browser flow | desktop bootstrap/auth | `temporary-compat` | canonical desktop bootstrap/session/auth contract | owner: Auth; remove only after canonical browser handoff/callback flow is deployed and old flow has zero consumers | `tests/cloud-contract/fixtures.ts` (`google-callback-missing-state`); `cloud/fixvox-proxy/src/index.ts` desktop OAuth handlers |
| `chat-completions` | `POST /v1/chat/completions` | Dictation Tauri | typed runtime action | `temporary-compat` | typed runtime action (`postprocess`, `selection_transform`, `assistant`) | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`chat-completions`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `usage-prewarm` | `POST /v1/usage/prewarm` | none in current Tauri/Control Room | legacy usage/prewarm | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`usage-prewarm`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `audio-transcriptions` | `POST /v1/audio/transcriptions` | Dictation Tauri | runtime transcription | `temporary-compat` | canonical runtime transcription contract | owner: Desktop; remove after released Tauri has zero calls to this route and canonical tests pass | `tests/cloud-contract/fixtures.ts` (`audio-transcriptions`); `src-tauri/src/fixvox_cloud.rs` and `runtime_transcription.rs` endpoint callers |
| `unknown-route-error` | `GET /fixture/not-found` | none in current Tauri/Control Room | operations | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`unknown-route-error`); Worker handler inventory in `cloud/fixvox-proxy/src/index.ts` |
| `usage-counter-reserve` | `POST /reserve` | none in current Tauri/Control Room | legacy usage/prewarm | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`usage-counter-reserve`); `cloud/fixvox-proxy/src/usage-admin.ts`/usage-counter fetch |
| `usage-counter-consume` | `POST /consume` | none in current Tauri/Control Room | legacy usage/prewarm | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`usage-counter-consume`); `cloud/fixvox-proxy/src/usage-admin.ts`/usage-counter fetch |
| `usage-counter-observe-prewarm` | `POST /observe-prewarm` | none in current Tauri/Control Room | legacy usage/prewarm | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`usage-counter-observe-prewarm`); `cloud/fixvox-proxy/src/usage-admin.ts`/usage-counter fetch |
| `usage-counter-prewarm-summary` | `GET /prewarm-summary` | none in current Tauri/Control Room | legacy usage/prewarm | `drop` | none | remove/leave unavailable in self-hosted target after fixture is retained only as historical evidence | `tests/cloud-contract/fixtures.ts` (`usage-counter-prewarm-summary`); `cloud/fixvox-proxy/src/usage-admin.ts`/usage-counter fetch |

## Scheduled Boundary (1/1)

| Boundary ID | Consumer | Capability | Disposition | Replacement / completion | Evidence |
| --- | --- | --- | --- | --- | --- |
| `scheduled-maintenance` | Worker cron only; no desktop/browser consumer | maintenance jobs | `redesign` | explicit provider-free job functions/timers; retain only named jobs with owner | `NON_HTTP_CONTRACT_FIXTURES`; `cloud/fixvox-proxy/src/scheduled-tasks.ts`; `cloud/fixvox-api/src/jobs.ts` |

## Bun/PostgreSQL Reuse Map

Current Bun routes are adapters, not automatic canonical paths. Reusable capabilities already present include strict config/health/readiness, device binding and published-profile resolution, hash-only OAuth/session repositories, RBAC authorization, quota ports/preflight, provider ports, Admin repositories, explicit jobs, redacted observability, and schema v4 repositories. Evidence: `cloud/fixvox-api/src/app.ts`, `composition.ts`, `postgres/*`, and `cloud/fixvox-core/src/*`.

## Explicit Exclusions And Ambiguities

- No unresolved current consumer remains. Exact routes with no Tauri or Control Room caller default to `drop` unless a product capability is explicitly retained as `redesign`.
- `GET /desktop/login` deliberately appears twice: valid start and invalid-flow scenario.
- Direct `/auth/google/*` device routes are legacy and dropped; `/callback` and `/desktop/google/start` remain temporary parts of the current desktop browser handoff.
- Control Room keeps its browser BFF contract. D1 consolidates backend routes by typed `/product/v1/control-room/*` domains without changing browser URLs.
- Discord, Telegram, embedded Admin, benchmark, recipe-policy, usage-prewarm, and usage-counter fetch APIs are outside the target by the approved product decision.
- D1R-1 reconciled the source inventory, disposition map, and alias ledger; D1R-2 closed the canonical apply schema and D1R-3 completed cross-document reconciliation. No runtime/client code or production was touched, and D2/Checkpoint E did not begin.
